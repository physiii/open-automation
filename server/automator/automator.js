class Automator {

	activateTrigger (data) {
		switch (data.type) {
			case 'time-of-day':
				break;
			case 'date':
				break;
			case 'state':
				break;
			case 'NFC':
				break;
			default:
				break;
		}
	}
}

module.exports = new Automation();
