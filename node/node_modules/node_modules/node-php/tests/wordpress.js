var express = require('express');
var php = require("../main"); 
var path = require("path"); 
var cluster = require("cluster"); 
var request = require("request"); 

var app = express();
	
app.use("/", php.cgi(__dirname+"/../example/wordpress")); 

app.listen(9090);
